name: ${PROJECT}
description: ${DESCRIPTION}
runtime: python
template:
  description: AWS base networking infrastructure (in Python)
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
