name: ${PROJECT}
description: ${DESCRIPTION}
runtime: nodejs
template:
  description: AWS base networking infrastructure
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
